# RiftLink — локальные пути (копируйте в .env.local и заполните)
# .env.local в .gitignore — не попадёт в репозиторий

# Путь к Flutter (bin/flutter.bat)
FLUTTER_ROOT=

# Путь к Android SDK (содержит platform-tools/adb.exe)
ANDROID_SDK_ROOT=
